/**
 * @deprecated Голосование теперь отображается в окне плеера (watch.ts).
 * Этот файл оставлен для совместимости, но не используется.
 */
export function initLobbyVoteNotifications(): void {
  // no-op: голосование теперь в плеере через IPC
}
